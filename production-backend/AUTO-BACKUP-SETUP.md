# 📦 Автоматични ежедневни backup-и - Инструкции

## 🎯 Какво прави системата

Автоматично прави backup на базата данни всеки ден в 23:00 (11 вечерта) и запазва последните 30 дни backup-и. Изпраща email нотификация след всеки backup.

## 🚀 Бърз старт

### 1. Инсталиране на зависимости (ако нямате nodemailer)

```bash
cd production-backend
npm install nodemailer
```

### 2. Настройка на email (в .env файл)

Добавете следните променливи в `.env` файла:

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Sales Scorecard <noreply@instorm.bg>
BACKUP_NOTIFICATION_EMAIL=your-email@example.com
```

**За Gmail:** Трябва да използвате "App Password", не обикновената парола.

### 3. Настройка на автоматични backup-и

```bash
cd production-backend
chmod +x setup-daily-backup.sh
./setup-daily-backup.sh
```

Това ще:
- Направи `auto-backup.sh` изпълним
- Добави cron job за ежедневен backup в 23:00 (11 вечерта)
- Изпраща email нотификация след всеки backup
- Покаже текущите cron jobs

### 2. Ръчно изпълнение на backup

Ако искате да направите backup ръчно:

```bash
cd production-backend
./auto-backup.sh
```

## 📋 Как работи

1. **Cron job** изпълнява `auto-backup.sh` всеки ден в 23:00 (11 вечерта)
2. **auto-backup.sh** извиква `backup-database.js` за създаване на backup
3. **Email нотификация** - изпраща email след всеки backup (успешен или неуспешен)
4. **Автоматично почистване** - изтрива backup-и по-стари от 30 дни
5. **Логове** - всички операции се записват в `backups/backup.log`

## 📁 Структура на backup-ите

```
production-backend/backups/
├── backup-2025-11-17T17-42-16/
│   ├── backup.json
│   ├── backup.sql
│   └── summary.txt
├── backup-2025-11-18T02-00-00/
│   └── ...
├── backup.log                    # Логове от всички backup-и
└── ...
```

## 🔍 Проверка на backup-ите

### Вижте последния backup:
```bash
ls -lth production-backend/backups/backup-* | head -1
```

### Вижте логове:
```bash
tail -f production-backend/backups/backup.log
```

### Вижте всички backup-и:
```bash
ls -lth production-backend/backups/backup-*
```

## ⚙️ Настройки

### Промяна на времето на backup

Редактирайте cron job:
```bash
crontab -e
```

Формат: `минута час * * * команда`

Примери:
- `0 23 * * *` - всеки ден в 23:00 (11 вечерта) - по подразбиране
- `0 22 * * *` - всеки ден в 22:00 (10 вечерта)
- `0 0 * * *` - всеки ден в 0:00 (полунощ)
- `0 */6 * * *` - на всеки 6 часа
- `0 23 * * 1-5` - всеки работен ден в 23:00

### Промяна на броя дни за запазване

Редактирайте `auto-backup.sh` и променете:
```bash
find "$BACKUP_DIR" -type d -name "backup-*" -mtime +30 -exec rm -rf {} \;
```

Променете `+30` на желания брой дни (напр. `+7` за 7 дни, `+90` за 90 дни).

## 🗑️ Премахване на автоматични backup-и

### Премахване на cron job:
```bash
crontab -e
# Изтрийте реда с auto-backup.sh
```

Или:
```bash
crontab -l | grep -v "auto-backup.sh" | crontab -
```

### Проверка дали е премахнат:
```bash
crontab -l | grep "auto-backup"
```

Ако няма изход, cron job-ът е премахнат.

## 📊 Мониторинг

### Проверка дали backup-ите се правят редовно:

```bash
# Вижте последните 10 backup-а
ls -lth production-backend/backups/backup-* | head -10

# Вижте логове за последните 7 дни
grep "$(date -v-7d '+%Y-%m-%d')" production-backend/backups/backup.log
```

### Проверка на размера на backup-ите:

```bash
du -sh production-backend/backups/backup-*
```

## 📧 Email нотификации

### Настройка

Email нотификациите изискват настройка на SMTP в `.env` файла:

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Sales Scorecard <noreply@instorm.bg>
BACKUP_NOTIFICATION_EMAIL=your-email@example.com
```

### Какво получавате в email-а

**При успешен backup:**
- ✅ Статус: Backup completed successfully
- 📁 Местоположение на backup-а
- 📊 Размер на backup-а
- 📈 Брой редове данни
- 📋 Списък с таблици и брой редове

**При неуспешен backup:**
- ❌ Статус: Backup failed
- 🔍 Детайли за грешката
- ⏰ Време на грешката

### Тестване на нотификации

```bash
# Тест на успешен backup
node send-backup-notification.js success '{"backupPath":"test-backup","backupSize":"1.2M","totalRows":"1432"}'

# Тест на неуспешен backup
node send-backup-notification.js failure '{"error":"Connection failed"}'
```

## 🔐 Безопасност

1. **Backup-ите съдържат чувствителни данни** - не ги споделяйте
2. **Логовете съдържат DATABASE_URL** - ограничете достъпа до `backup.log`
3. **Email паролите** - използвайте App Passwords, не обикновени пароли
4. **Регулярно тествайте възстановяването** - проверете дали backup-ите работят

## 🆘 Troubleshooting

### Backup-ът не се изпълнява

1. Проверете cron job:
   ```bash
   crontab -l
   ```

2. Проверете логове:
   ```bash
   tail -50 production-backend/backups/backup.log
   ```

3. Тествайте ръчно:
   ```bash
   cd production-backend
   ./auto-backup.sh
   ```

### Грешка с DATABASE_URL

Уверете се, че `DATABASE_URL` е зададен:
```bash
# В .env файл или
export DATABASE_URL="postgresql://..."
```

### Няма достатъчно място

Проверете дисковото пространство:
```bash
df -h
```

Намалете броя дни за запазване на backup-ите.

## 📝 Примери

### Backup всеки 6 часа:
```bash
0 */6 * * * cd /path/to/production-backend && ./auto-backup.sh >> /path/to/backups/backup.log 2>&1
```

### Backup всеки понеделник и четвъртък:
```bash
0 2 * * 1,4 cd /path/to/production-backend && ./auto-backup.sh >> /path/to/backups/backup.log 2>&1
```

## ✅ Checklist за настройка

- [ ] Изпълнете `./setup-daily-backup.sh`
- [ ] Проверете че cron job е добавен: `crontab -l`
- [ ] Тествайте ръчно: `./auto-backup.sh`
- [ ] Проверете логове: `tail -f backups/backup.log`
- [ ] Проверете че backup файлове се създават: `ls backups/backup-*`

