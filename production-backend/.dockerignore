node_modules
**/node_modules
npm-debug.log
.DS_Store
.git
.gitignore

# Large backup artifacts not needed in runtime image
backups
**/backups
*.sql
*.tar
*.tar.gz
*.gz

# Local/dev docs and reports
*.md

# Admin: only exclude deps and local build output — src/ and public/ are required for the admin-builder stage
admin-panel/node_modules
admin-panel/build

# Do not copy a host-built bundle into the image; Dockerfile stage `admin-builder` is canonical
public/react-admin

# Legacy backup dirs
SalesScorecard-Backup-*
SalesScorecard-Backup-*/**
