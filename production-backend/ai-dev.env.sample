# Copy lines into your local .env (do not commit real keys).
# Enables OpenAI proxy for local prompt experiments — not model training.

AI_DEV_ENABLED=true
OPENAI_API_KEY=

# Optional: require curl/header Authorization: Bearer <token> for /dev/ai/*
# AI_DEV_BEARER_TOKEN=your-long-random-secret
